# Handoff: S6 — Release (add-unit-tests)
Generated by: developer | Date: 2026-06-30

CPP baton — final S6 entry. Change is archived after this handoff.

## 1. Implementation Patterns

The release artifacts for this change follow these patterns:

- **No-migration release**: no DB, no deps, no config — release.md migration section is entirely N/A except for the docs-sync advisory (context/stack.md + CLAUDE.md "no automated tests" lines become stale).
- **WASM cache-purge is the only deploy gotcha**: `find ~/.cache/zellij -type d -name 'zellij-claude-sync.wasm' -prune -exec rm -rf {} +` must run after copying the new `.wasm`. Documented in release.md §Post-Deploy Smoke Test Step 2 and in CLAUDE.md §Redeploying.
- **CI gate is the merge gate**: `ci.yml` runs on the PR. CI green = safe to merge. No staging environment exists (developer tooling, single user).
- **Rollback is a clean `git revert`**: three files changed (`src/enrich.rs` new, `src/main.rs` modified, `.github/workflows/ci.yml` new); `release.yml` untouched; no migrations to reverse.

## 2. Deviations

- **DEV-001 (MINOR, behavior-neutral)**: `src/main.rs` required `#[cfg(not(test))]` guards around `use zellij_tile`, `struct State`, `impl ZellijPlugin`, and `register_plugin!` to isolate WASI host symbols from the native test linker. Transparent to the `wasm32-wasip1` build target. ADR-002 did not anticipate the linker isolation requirement; the idiomatic Rust solution was applied without design change.
- **IMPL-003 (signature, behavior-neutral)**: `enrich_nodes` signature narrowed from `&mut Vec<KdlNode>` to `&mut [KdlNode]` to satisfy `clippy::ptr_arg -D warnings`. All callers coerce transparently via `Deref`.

No major deviations. No design or spec gaps surfaced during S6.

## 3. Inferred / Advisory

- `context/stack.md` line "Test framework: None" and "Coverage gate: N/A" are stale post-merge. The `cargo test` suite (33 tests) and `ci.yml` make these lines inaccurate. Recommend a follow-on docs-sync commit.
- `CLAUDE.md` "There are no automated tests" line is similarly stale. Recommend updating in the same docs-sync commit.
- `absolute_cwd_passthrough_enriched_br_tests_008` discards `_result` (resolver-call assertion covers cwd resolution but not enrichment output for this path). Low risk; should be strengthened in a follow-on change.

## 4. Where QA Should Focus (reading order for re-review or follow-on)

For any future change touching `src/enrich.rs`:
1. `openspec/changes/add-unit-tests/dev-test-report.md` — full build/test evidence, AC coverage table, design deviations.
2. `src/enrich.rs` — the pure module + inline 33-test suite; all tests name their AC-ID.
3. `src/main.rs` — the `#[cfg(not(test))]` guard pattern (DEV-001); the synchronous `pipe()` flow.
4. Skip: `hooks/session-marker.py`, `shell/snap.*`, `install.sh`, `release.yml` — all unchanged by this change.

## 5. Reading Order for S6 Artifacts

1. `openspec/changes/add-unit-tests/release.md` — release notes, migration checklist, rollback plan, post-deploy smoke, deploy strategy.
2. `openspec/changes/add-unit-tests/qa-report.md §13` — GO decision rationale and final bug list (F-001 CLOSED).
3. `openspec/changes/add-unit-tests/dev-test-report.md §Summary` — implementation evidence.
4. `openspec/changes/add-unit-tests/_decisions.jsonl` — full audit trail (IMPL-001 through IMPL-006, ADR-001 through ADR-005, DEV-001, F-001 lifecycle).
