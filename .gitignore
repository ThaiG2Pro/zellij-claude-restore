/target

# Local tooling / editor indexes (machine-specific, not project source)
/.codegraph
/.cursor

# Transient SDLC-kit backups (machine-local; regenerated by the kit, never source)
/.snapshots
**/*.pre-migration-backup

# >>> kiro-sdlc-kit >>>
# Kit-owned: regenerated by `kiro-sdlc-init --force`. Delete this block to commit the kit.
# (context/, openspec/, docs/, memory/ are intentionally NOT ignored — commit those.)
.claude/
.kiro/
/sdlc.config.json
/pipelines.json
# <<< kiro-sdlc-kit <<<
