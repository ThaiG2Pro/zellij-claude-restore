[package]
name = "zellij-claude-sync"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
zellij-tile = "0.44.0"
kdl = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "s"   # optimize for size (WASM)
